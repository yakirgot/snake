# @snake/models

Shared types and constants used across the Snake workspace, including branded coordinates, direction types, DOM selectors, and key codes.

**Nx project name:** `shared-models`

## Commands

```bash
npx nx build shared-models
npx nx lint shared-models
npx nx typecheck shared-models
```
