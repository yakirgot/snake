# @snake/domain

Client-side domain state for the Snake game: `GameState`, game settings, sound settings, and high score.

Managed with [tsyringe](https://github.com/microsoft/tsyringe) dependency injection.

**Nx project name:** `domain`

## Commands

```bash
npx nx build domain
npx nx test domain
npx nx lint domain
npx nx typecheck domain
```
