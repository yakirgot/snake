# @snake/rendering

UI rendering helpers for the Snake game: canvas setup and drawing, DOM utilities, and the `UIManager` for score and announcements.

**Nx project name:** `rendering`

## Commands

```bash
npx nx build rendering
npx nx test rendering
npx nx lint rendering
npx nx typecheck rendering
```
