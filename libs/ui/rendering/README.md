# rendering

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build rendering` to build the library.

## Running unit tests

Run `nx test rendering` to execute the unit tests via [Vitest](https://vitest.dev/).
