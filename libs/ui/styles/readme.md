# @snake/styles

Global CSS for the Snake app (colors and base styles). Import this package once from the app entry point to load the styles.

**Nx project name:** `ui-styles`

## Commands

```bash
npx nx lint ui-styles
npx nx stylelint ui-styles
npx nx typecheck ui-styles
```
