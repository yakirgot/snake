commit-msg:
  commands:
    "lint commit message":
      run: npx commitlint --edit

pre-push:
  commands:
    "run all checks":
      run: npx nx affected --parallel -t typecheck lint stylelint test test:e2e
